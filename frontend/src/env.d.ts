// Types for process.env.EXPO_PUBLIC_* used in src/config.ts.
// Lives in src/ (not root expo-env.d.ts) because the Expo CLI manages/removes
// the root file on `expo start` when it thinks it's unneeded.
/// <reference types="expo/types" />
